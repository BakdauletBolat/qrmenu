server{
    listen 80;
    listen [::]:80;
    server_name easymenufront.bolatb.kz www.easymenufront.bolatb.kz;
    root /var/www/qrmenu/qr-front/dist;
    location / {
        include  /etc/nginx/mime.types;
        try_files $uri $uri/ /index.html;
    }
}
